% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette_prob_bg.R
\name{point_discrim_prob}
\alias{point_discrim_prob}
\title{Point-mark discrimination probability for a pair of colors}
\usage{
point_discrim_prob(hex1, hex2, size = seq(0.25, 2, by = 0.05))
}
\arguments{
\item{hex1, hex2}{two hex color strings, e.g. \code{"#3B4CC0"} and \code{"#B40426"}}

\item{size}{numeric vector of point diameters in degrees of visual angle.
Szafir's tested range was 0.25 to 2.0 degrees; see the validated-range
caveat above for values outside that range.}
}
\value{
data.frame with columns \code{size_deg} and \code{p_discriminable}
}
\description{
The point-mark analogue of \code{\link[=line_discrim_prob]{line_discrim_prob()}}: predicts the proportion
of viewers who would detect the color difference between two circular
scatterplot-style point marks, as a function of point diameter, using the
model fit by Szafir (2018) for scatterplots (Experiment One; see Details).
}
\details{
Model: \code{p = m_x(s) * dx}, \code{m_x(s) = c_x + k_x / s}, per CIELAB axis (L*,
a*, b*), combined across axes as \code{sqrt(sum((dx * m_x(s))^2))} and clipped
to \verb{[0, 1]} -- identical form to \code{\link[=line_discrim_prob]{line_discrim_prob()}}, but with its own
coefficients fit on point marks, and \code{s} is point \emph{diameter} rather than
line thickness.

\strong{Caveats} (see \code{\link[=line_discrim_prob]{line_discrim_prob()}} for the shared ones -- white
background only, cross-axis combination untested):
\itemize{
\item \strong{Validated range.} Fit using six diameter steps from 0.25 to 2.0
degrees. This is a much higher floor than the line model's 0.05-0.35
degrees -- \strong{do not} feed sizes below roughly 0.25 degrees into this
function. Below about 0.1-0.2 degrees (depending on axis), \code{c_x + k_x/s}
goes negative, which is not a meaningful extrapolation, just a sign
flip from being far outside the fitted domain.
\item \strong{Points are less discriminable than lines of the same size.} Szafir's
own comparison found colors generally more discriminable on elongated
marks (lines, bars) than on points -- don't expect a point and a line of
the same visual-angle size to give similar probabilities for the same
color pair.
}
}
\references{
Szafir, D.A. (2018). Modeling Color Difference for
Visualization Design. IEEE Transactions on Visualization and Computer
Graphics, 24(1), Experiment One.
}
\seealso{
\code{\link[=line_discrim_prob]{line_discrim_prob()}}, \code{\link[=palette_prob_bg]{palette_prob_bg()}}
}
