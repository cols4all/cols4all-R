% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette_prob_bg.R
\name{palette_prob_bg}
\alias{palette_prob_bg}
\title{Pairwise mark-discrimination probability matrix for a palette}
\usage{
palette_prob_bg(
  x,
  bgcol = "#FFFFFF",
  cvd = NULL,
  severity = 1,
  thickness = 0.05,
  mark = c("line", "point"),
  metric = 2000,
  lum_k = 100,
  lum_floor = 0.05,
  lum_adjust = FALSE
)
}
\arguments{
\item{x}{vector of hex colors (the palette)}

\item{bgcol}{background color as a hex string. Only used when
\code{lum_adjust = TRUE}.}

\item{cvd}{type of color vision deficiency to simulate before computing
probabilities, or \code{NULL} for none. See \code{colorblindcheck::palette_dist()}
for supported values.}

\item{severity}{severity of the CVD simulation, between 0 and 1}

\item{thickness}{mark size in degrees of visual angle -- line thickness
when \code{mark = "line"}, point diameter when \code{mark = "point"} -- passed to
the underlying \code{\link[=line_discrim_prob]{line_discrim_prob()}} / \code{\link[=point_discrim_prob]{point_discrim_prob()}} model. See
their documentation for the (different) validated ranges.}

\item{mark}{which Szafir (2018) model to use: \code{"line"} (line graphs) or
\code{"point"} (scatterplots). See \code{\link[=line_discrim_prob]{line_discrim_prob()}} and
\code{\link[=point_discrim_prob]{point_discrim_prob()}}.}

\item{metric}{accepted for signature parity with \code{palette_dist_bg()}, but
unused: this model works directly on CIELAB L*/a*/b*, not on a
\code{colorblindcheck::palette_dist()} distance.}

\item{lum_k, lum_floor}{passed to \code{lum_factor()} when \code{lum_adjust = TRUE};
see that function's documentation for their meaning.}

\item{lum_adjust}{should the probability be multiplied by \code{lum_factor()}?
Exploratory and unvalidated -- see Details.}
}
\value{
n x n matrix of discrimination probabilities as percentages
(0-100), with \code{NA} on the diagonal
}
\description{
The probability analogue of \code{palette_dist_bg()}'s \code{"min-lum"} method: for
every pair of colors in \code{x}, the probability that two \code{size}-sized marks
(line marks, or point/scatterplot marks) drawn in those colors would be
told apart, using the Szafir (2018) mark-discrimination models (see
\code{\link[=line_discrim_prob]{line_discrim_prob()}} / \code{\link[=point_discrim_prob]{point_discrim_prob()}}) instead of a
\code{colorblindcheck::palette_dist()} distance.
}
\details{
Only \code{"min-lum"}'s weakest-link-plus-lightness-penalty idea is mirrored
here, since it is the only \code{palette_dist_bg()} method with a coherent
probability interpretation: \code{"bg-norm"} and \code{"bg-norm-cr-spread"} are
distance-space transforms (squared-distance ratios, log-contrast spread)
that don't map onto a \verb{[0, 1]}-bounded probability, so there is no
\code{method} argument here.

Unlike \code{palette_dist_bg()}, this function does \strong{not} compute a
mark-vs-background discriminability term (the \code{bdiff} piece of
\code{"min-lum"}). Szafir's models were fit on two thin/small marks compared
against each other on a field of gray distractors, not a mark against a
large uniform background field, so reusing that ΔE2000-based \code{bdiff}
logic here is not well founded. \code{lum_adjust = TRUE} is offered as an
exploratory alternative instead (see below) -- if a background-visibility
floor is wanted for this model, it needs its own justification, not a
straight port from the distance version.

\strong{Caveats}, in addition to those on \code{\link[=line_discrim_prob]{line_discrim_prob()}} /
\code{\link[=point_discrim_prob]{point_discrim_prob()}} (note the two marks have different validated size
ranges -- 0.05-0.35 degrees for \code{"line"}, 0.25-2.0 degrees for \code{"point"}):
\itemize{
\item \strong{\code{lum_adjust = TRUE} is exploratory, not validated.} It multiplies the
Szafir-derived probability by \code{lum_factor()}, which was fit on ΔE-based
\emph{distance} data, not probabilities. There is no empirical basis yet for
combining the two this way -- treat it as a hypothesis to test, not a
trustworthy correction. It defaults to \code{FALSE}.
}
}
\seealso{
\code{\link[=line_discrim_prob]{line_discrim_prob()}}, \code{\link[=point_discrim_prob]{point_discrim_prob()}}
}
