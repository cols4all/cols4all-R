% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette_prob_bg.R
\name{visual_angle_to_px}
\alias{visual_angle_to_px}
\title{Convert a line thickness from degrees of visual angle to pixels}
\usage{
visual_angle_to_px(
  deg,
  viewing_distance_in = 30,
  dpi = 96,
  convention = .C4A$angle_convention
)
}
\arguments{
\item{deg}{line thickness in degrees of visual angle}

\item{viewing_distance_in}{viewing distance, in inches}

\item{dpi}{display resolution, in dots (pixels) per inch}

\item{convention}{\code{"szafir"} or \code{"standard"} -- see Details. Defaults to
the \code{angle_convention} package option (see \code{\link[=c4a_options]{c4a_options()}}).}
}
\value{
line thickness in pixels
}
\description{
The defaults (30 inch viewing distance, 96 dpi) match Szafir (2018)'s own
assumed viewing conditions. Override both to match an actual lab setup
(measured pixel pitch and viewing distance), rather than trusting a
reported dpi, which is frequently wrong for actual displays.
}
\details{
The standard chord formula for the linear size subtending a visual angle
\code{deg} at distance \code{d} is \code{2 * d * tan(deg/2)} -- this is what \code{convention = "standard"} computes. Szafir (2018)'s own in-text px-equivalents (e.g.
"0.25 deg (6px)", "2.0 deg (50px)" for points; "0.05 deg (1px)", "0.35 deg
(9px)" for lines) are consistently exactly \emph{half} of that formula's result
at their own stated 30in/96dpi assumptions -- across both experiments, at
four independently checked (angle, px) pairs, which is why this is treated
as their own conversion convention rather than a one-off rounding
difference. \code{convention = "szafir"} (the default, and the package default
via \code{c4a_options("angle_convention")}) reproduces that, so preview sizes
in \code{\link[=palette_prob_bg]{palette_prob_bg()}} / \code{c4a_gui()} visually match the paper's own
figures; \code{convention = "standard"} gives the textbook-correct value, which
is what you want once calibrating against a real measured display.
}
\seealso{
\code{\link[=px_to_visual_angle]{px_to_visual_angle()}}, \code{\link[=line_discrim_prob]{line_discrim_prob()}}, \code{\link[=c4a_options]{c4a_options()}}
}
